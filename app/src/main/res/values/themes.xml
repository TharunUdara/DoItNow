<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoItNow" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:textColorHint">#64748B</item> <!-- Medium slate for better visibility -->
        <item name="android:editTextColor">@color/text_dark</item>
    </style>

    <style name="Theme.DoItNow" parent="Base.Theme.DoItNow" />
</resources>